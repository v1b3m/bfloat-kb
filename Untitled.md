- I should be able to collapse any panel to focus on one, maybe show just the diff or just the graph. 

---


Initial 3 items we were tackling:

  1. Working-tree diffs (staged/unstaged mode)
  2. Graph edge routing improvements (GitKraken-like)
  3. Virtualization/performance for large repos (graph + diff)

  New items you added:

  4. Collapsible diff cards
  5. Recents section for recently opened repos
  6. Theme support

---

## Prioritized

1. Collapsible diff cards
2. Recents section for recently opened repos
3. I should be able to collapse any panel to focus on one, maybe show just the diff or just the graph. 
4. Working-tree diffs (staged/unstaged mode)
5. Graph edge routing improvements (GitKraken-like)
6. Virtualization/performance for large repos (graph + diff)
7. Theme support
8. Merge commits show no diff, this is not correct

---

1. Showing the full path to the repo in recents may not add much value to me, perhaps hover should show the full path instead
2. Can we have the option where I view changes on a specific branch
3. I have not tested this with real live changes, but how is it handling file/commit changes in real-time, does it auto-refresh, do we need a refresh button?
4. The hover effects seem to work "when they feel like". I hover over buttons and nothing happens.
5. Let's compact the graph commits some more, that vertical spacing is a lot
6. Can we add a shadow effect (a vertical line) indicating where the commit messages start?
7. We need thinner scrollbars
8. 