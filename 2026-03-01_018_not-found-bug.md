**Ref:** main
**Depends:** 2026-03-01_004_agent-not-found

### Context

The bug in `2026-03-01_004_agent-not-found` is still happening. However, I've found an issue. As soon as you exit the project page, return immediately, and send a message right way. It happens. It's probably because the app has not yet done proper cleanup/initializeion.

### Acceptance Criteria

- [ ] Not Found bug should never happen

### Constraints

- None

### Resources

- None
## Handoff
- **What was done:** Hardened `useLocalAgent` stale-session recovery for fast project-page re-entry by treating generic `Not Found` prompt failures as recoverable session loss, clearing stale resume refs, and forcing one fresh non-resume session creation before re-enabling resume.
- **Commit:** adf8e74
- **Files touched:** `_SCRATCH.md`, `app/hooks/useLocalAgent.ts`
- **Decisions made:** Kept fix local to renderer hook/session lifecycle (no sidecar API contract changes) and added a one-cycle resume skip guard so recovery cannot immediately retry the same stale resume ID.
- **Known limitations:** Recovery still retries once; if a different non-session 404 is ever returned by prompt path, it now triggers session recreation (low risk for this endpoint).
- **How to verify:** 1) Open a project, send a message. 2) Leave project page and immediately return. 3) Send another message right away. 4) Confirm no `Not Found` toast appears and message is sent via recovered/new session.
