---
picked_at: 2026-03-04T08:12:09Z
picked_mode: execute
ref: main
depends_on: none
---

### Context

A user trying to clone a repository run into this bug:

```
Git Error

Failed to clone repository after trying 1 URL(s). 1. authenticated URL: Command failed: git clone 'https://x-access-token:ghs_Xdzdd0ryTfQj7hZQp83XealTgMAAbf1oXOMI@github.com/bfloat-projects/2520b5a1-8f5e-4c07-8221-5679b04c52c5.git

Users/adentrano/.bfloat/projects/2520b5a1-8f5e-4c07-8221-5679b04c52c5' xcode-select: note: No developer tools were found, requesting install. If developer tools are located at a non-default location on disk, use 'sudo xcode-select --switch path/to/Xcode.app' to pecify the Xcode that you wish to use for command line developer tools, and cancel the installation dialog. See 'man xcode-select for more details.

```
### Clarified Context

- Task base ref for this worktree is `main` (this note currently says `develop`; this should be treated as stale metadata).
- Failure happens during desktop clone flows when `git` is unavailable on macOS because Apple Command Line Tools are not installed.
- Reported error currently includes noisy shell output and a tokenized URL in the message shown to the user, which is both poor UX and a credential-exposure risk.
- Relevant clone paths already funnel through shared clone-failure formatting:
  - `apps/desktop/lib/main/project-service.ts`
  - `apps/desktop/lib/main/git-ops/index.ts`
  - `apps/desktop/lib/conveyor/handlers/github-import-handler.ts`
  - `apps/desktop/lib/conveyor/handlers/local-import-handler.ts`
  - `apps/desktop/lib/git/clone-strategy.ts`
- Existing logic in `clone-strategy.ts` attempts to normalize/redact errors and detect git-unavailable cases (`xcode-select`, missing git). Discovery should validate whether all runtime error shapes are covered.

### Acceptance Criteria

- [x] On macOS without Command Line Tools, clone failure is shown as a short actionable message (no raw shell command dump).
- [x] User-visible clone errors never contain embedded credentials/tokens, regardless of failure path.
- [x] All desktop clone entry points emit consistent error behavior (project open, git ops clone, conveyor imports).
- [x] Git-unavailable detection is reliable for known stderr variants (`xcode-select`, invalid developer path, command-not-found).
- [x] Behavior is covered by automated tests for clone error normalization/classification.
- [x] Discovery decisions below are resolved and captured before implementation.
### Constraints

- Discovery mode only for this task step; no production code changes in this pass.
- Must preserve existing cross-platform behavior (macOS, Windows, Linux), including bundled MinGit behavior on Windows.
- Must not regress clone fallback strategy (authenticated URL, remote URL, SSH fallback) when git is available.
- Error handling must prioritize credential redaction before surfacing/logging messages.

### Risks

- Message parsing may miss variant stderr formats and leak command fragments.
- Over-broad matching can misclassify genuine auth/network failures as git-unavailable.
- Different clone call sites may drift and produce inconsistent user messages.
- Logging paths outside user-facing errors may still expose tokenized URLs if not audited.

### Options

- Option A: Improve stderr normalization only (lowest implementation effort, weaker prevention).
- Option B: Add explicit preflight checks for git availability on startup/clone (strong UX, extra platform branching).
- Option C (recommended): Hybrid approach:
  - preflight check before clone attempts,
  - keep robust normalization/redaction as fallback defense,
  - unify error code/message contract across clone entry points.

### Explicit User Decisions Needed

- [x] Should clone fail fast with a dedicated "Git not installed" state before any URL attempts, or continue current attempt-first behavior?
	- [x] Fail fast with a git availability preflight (`git --version`) before clone URL attempts.
- [x] Should UI include a direct action for macOS setup guidance (for example, "Run `xcode-select --install`") versus text-only guidance?
	- [x] Include direct command guidance in the user-visible error message.
- [x] For git-unavailable errors, should SSH fallback be skipped immediately (recommended) or still attempted?
	- [x] Skip SSH fallback immediately once git-unavailable is classified.
- [x] Do we need telemetry for classified clone failure types (git-unavailable, auth, network) in this iteration?
	- [x] Not added in this iteration; can be introduced in a follow-up without altering the user-facing contract.

### Implementation Notes (Execute)

- Added git preflight checks and early-stop logic in all desktop clone entry points:
  - `apps/desktop/lib/main/project-service.ts`
  - `apps/desktop/lib/main/git-ops/index.ts`
  - `apps/desktop/lib/conveyor/handlers/github-import-handler.ts`
  - `apps/desktop/lib/conveyor/handlers/local-import-handler.ts`
- Hardened shared clone error normalization/classification in `apps/desktop/lib/git/clone-strategy.ts`:
  - credential redaction
  - concise actionable messaging for git-unavailable
  - detection coverage for `xcode-select`, invalid developer path, command-not-found, and spawn `ENOENT`
- Added automated tests in `apps/desktop/lib/git/clone-strategy.test.ts` and validated with:
  - `pnpm dlx tsx --test lib/git/clone-strategy.test.ts`

### Resources

- User-reported error sample in task context.
- Clone flow sources listed above (`apps/desktop/lib/...`).

---


## Handoff
- **What was done:** Implemented clone preflight + error normalization for git-unavailable scenarios across desktop clone entry points, with credential redaction and consistent fail-fast behavior that skips fallback attempts when git is missing.
- **Commit:** `f0e02844`
- **Files touched:** `apps/desktop/lib/git/clone-strategy.ts`, `apps/desktop/lib/git/clone-strategy.test.ts`, `apps/desktop/lib/main/project-service.ts`, `apps/desktop/lib/main/git-ops/index.ts`, `apps/desktop/lib/conveyor/handlers/github-import-handler.ts`, `apps/desktop/lib/conveyor/handlers/local-import-handler.ts`
- **Decisions made:** Kept fallback URL strategy when git is available; for classified git-unavailable failures (`xcode-select`, invalid developer path, command-not-found/ENOENT), return concise actionable guidance and stop additional clone retries.
- **Known limitations:** Classification is pattern-based; uncommon platform-specific stderr variants may require adding new patterns.
- **How to verify:** Run `pnpm --filter @bfloat/workbench exec node --test --import tsx lib/git/clone-strategy.test.ts` and manually simulate missing git/Command Line Tools on macOS to confirm user-facing clone error is concise and token-safe.
