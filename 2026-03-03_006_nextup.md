```
I can see the error is related to navigation components. The issue is likely with the `Link` components using `asChild` with `TouchableOpacity`. Let me fix this by using `router.push()`instead, which is cleaner and more reliable.
```

 `Link` components using `asChild` with `TouchableOpacity` should be prohibited explicitly
