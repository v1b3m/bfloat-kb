**Ref:** chore/day-3
**Depends:** 2026-03-01_005_mcp-tools-not-loading

### Context

![[Pasted image 20260301111654.png]]

The message `Use the /add-revenuecat skill to set up RevenueCat in-app purchases for this project` is now being sent twice.

---

Also the banner that says "Set up Revenuecat" does not show up unless I exit the chat back home and return.

It should also change text to "Setting up Revenuecat..." and be disabled after the subsequent message is sent
### Acceptance Criteria

- [ ] No duplicate messages are sent
- [ ] Banner is correctly disabled and text update while subsequent message is sent
- [ ] Revenuecat messages are detected without having to first leave project page and returning

### Constraints

- None

### Resources

- Check the commits in this branch, they should guide you on the changes we've made
- This task is a continuation of [[2026-03-01_005_mcp-tools-not-loading]]
- 

## Progress
- [x] Traced duplicate RevenueCat setup prompt dispatch to non-idempotent pending-prompt effect execution.
- [x] Updated RevenueCat setup banner flow to dispatch through `workbenchStore.triggerChatPrompt(...)` so setup state is immediate and bound to pending prompt lifecycle.
- [x] Added request-ID guard in pending prompt effect to prevent duplicate sends for the same queued prompt.
- [x] Verified lint on touched file: `pnpm eslint app/components/chat/Chat.tsx` (warnings only, no errors).

## Outcome
- [x] No duplicate messages are sent for the same pending RevenueCat setup request.
- [x] RevenueCat banner transitions to "Setting up RevenueCat..." and button is disabled while setup prompt is being processed.
- [x] RevenueCat setup prompt handling now runs through the same immediate in-page pending-prompt path (no navigation away/back required for detection).

## Handoff
- **Files touched:** `app/components/chat/Chat.tsx`
- **Summary:** RevenueCat "Use" action now uses pending prompt queue; pending prompt consumer is deduped per request ID.
