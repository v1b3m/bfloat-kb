**Ref:** develop
**Depends:** none

### Context

During adding of stripe to web apps, I often run into this bug. We need to patch the skill so we don't run into this again.

```
 GET /api/plans 200 in 1277ms
 ⚠ Unsupported metadata viewport is configured in metadata export in /. Please move it to viewport export instead.
Read more: https://nextjs.org/docs/app/api-reference/functions/generate-viewport
 GET / 200 in 71ms
 ⚠ Unsupported metadata viewport is configured in metadata export in /pricing. Please move it to viewport export instead.
Read more: https://nextjs.org/docs/app/api-reference/functions/generate-viewport
 GET /pricing 200 in 35ms
 GET /api/plans 200 in 373ms
 ✓ Compiled /api/checkout in 191ms (768 modules)
Checkout session creation failed: [Error: Invalid URL: An explicit scheme (such as https) must be provided.] {
  type: 'StripeInvalidRequestError',
  raw: [Object],
  rawType: 'invalid_request_error',
  code: 'url_invalid',
  doc_url: 'https://stripe.com/docs/error-codes/url-invalid',
  param: 'success_url',
  detail: undefined,
  headers: [Object],
  requestId: 'req_2XIxCrC7Ns9ZbZ',
  statusCode: 400,
  userMessage: undefined,
  charge: undefined,
  decline_code: undefined,
  payment_intent: undefined,
  payment_method: undefined,
  payment_method_type: undefined,
  setup_intent: undefined,
  source: undefined
}
 POST /api/checkout 500 in 1800ms
```

### Acceptance Criteria

- [ ] Stripe is connected e2e

### Constraints

- None

### Resources

- None
- 
## Handoff
- **What was done:** Updated the `add-stripe` skill guidance to build Stripe checkout `success_url`/`cancel_url` as absolute URLs using a validated `NEXT_PUBLIC_SITE_URL` override with `request.url` origin fallback; added `NEXT_PUBLIC_SITE_URL` (with scheme requirement note) to Stripe env template.
- **Commit:** `7b1c41c`
- **Files touched:** `resources/skills/add-stripe/SKILL.md`, `resources/skills/add-stripe/templates/env.example`
- **Decisions made:** Patched the skill source-of-truth/docs instead of app code so future Stripe integrations generated by the IDE avoid this recurring `url_invalid` checkout failure.
- **Known limitations:** This task updates skill guidance/templates only; existing already-generated apps still need equivalent route logic if they currently build callback URLs unsafely.
- **How to verify:** Regenerate/apply Stripe integration from the skill and confirm checkout session creation succeeds without `Invalid URL` errors even when `NEXT_PUBLIC_SITE_URL` is omitted (uses request origin) and when set (must be absolute with scheme).
