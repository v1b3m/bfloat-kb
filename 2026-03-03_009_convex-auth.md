**Ref:** chore/day-5-001
**Depends:** none

### Context

The "Convex + Auth" path when connecting convex is supposed to queue the auth follow up message to be sent to the agent after convex is wired up. This is EXTREMELY BRITTLE and does not work at all. It fails to detect that convex has been wired up so the auth never gets connected.

I also believe the workbench does not get updates in real-time. I can see changes to files when I directly open the folder in an IDE that I do not see in the workbench.

We need to have a reliable way to configure the setup and then auth after.
### Acceptance Criteria

// Fill this section up

- [ ] 

### Constraints

- None

### Resources

- None




## Handoff
- **What was done:** Hardened Convex + Auth sequencing so auth follow-up no longer depends solely on stale workbench file state. Added bootstrap detection from project file tree and a bounded refresh fallback that rescans tree and auto-queues `/convex-auth` only after Convex bootstrap artifacts are detected.
- **Commit:** `6c8af40`
- **Files touched:** `app/components/chat/Chat.tsx`, `app/lib/integrations/convex.ts`, `_SCRATCH.md`
- **Decisions made:** Avoided broad realtime watcher implementation in this task; used targeted tree-refresh fallback to make Convex+Auth reliable with existing bridge limitations.
- **Known limitations:** Global project realtime sync is still constrained by `projectFiles.onFileChange` being a no-op bridge stub; this change specifically hardens Convex+Auth auto-follow-up.
- **How to verify:** In chat, choose Convex -> Convex + Auth on a project missing Convex bootstrap files. Run setup. After setup writes `convex/schema.ts` + `_generated` artifacts + at least one Convex function file, UI should auto-trigger `/convex-auth` (toast: "Convex setup complete. Starting auth setup...").