All apps use the same OAuth client (for all apps), this sounds hella insecure.

Let all apps use better auth for now.

---

When I switch tabs between active sessions, the streaming indicator (the stop stream button, etc.) does not show up. The chat continues streaming, however, to completion. We need to know if the current session is streaming, and correct render that to the user.

---

