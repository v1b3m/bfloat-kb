Run the build as a background process, this way if a user closes the model, the build continues in the background and is not killed prematurely.

- `orphaned` vs `killed` processes
- Mobile `revenueCat`
- Web `StripeConnect`
- 
