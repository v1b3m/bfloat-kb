**Ref:** develop
**Depends:** none

### Context


> [!important] NOT MERGED
> This is not merged as the agent will use the MCP tools automatically if and when they're available.


For integrations with mcp tools (revenuecat, stripe, etc). We added these tools so we use them. So, ideally after connecting the integration. The agent should not ask the user to go create products if the agent can directly pull this off with tools. The agent should connect, list/create products, the user should just direct the agent to make some changes or something in this direction. We are aiming for autonomous agents that don't need to be spoon-fed with each detail.

### Acceptance Criteria

- [ ] Products, Offerings, Prices are automatically fetched, listed, created unless otherwise

### Constraints

- None

### Resources

- None


## Handoff
- **What was done:** Added MCP-first integration autonomy guidance to runtime system prompt and managed AGENTS/CLAUDE instruction generation so agents default to listing/creating Stripe/RevenueCat products, prices, and offerings via MCP tools instead of asking users for manual dashboard setup.
- **Commit:** `04d6038`
- **Files touched:** `lib/launch/system-prompt.ts`, `packages/sidecar/src/services/agent-instructions.ts`
- **Decisions made:** Implemented this as instruction-layer behavior (prompt/template) rather than MCP server wiring since Stripe/RevenueCat MCP auto-config already exists and task scope is autonomous agent behavior.
- **Known limitations:** Behavior still depends on MCP availability/permissions; if tools are unavailable or blocked, agents may still require manual user steps.
- **How to verify:** Start a new agent session with Stripe or RevenueCat connected and ask to set up pricing; confirm it first lists existing MCP resources and creates missing products/prices/offerings without asking the user to do dashboard setup.
