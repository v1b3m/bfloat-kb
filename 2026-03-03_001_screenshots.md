**Ref:** develop
**Depends:** 2026-03-02_010_wrong-screenshots

### Context

I've noticed the screenshots taken will be right if the route in the browser URL is correct.

We've worked on navigation challanges in [[2026-03-02_011_web-navigation-problem-001]]. However, I notice that when I click a link in an app that say navigates to `/pricing`, the page in the browser can change to the correct content but the URL will stay at `/`. Taking a screenshot now will render the page at `/` and not the page rendered in the browser window which can be confusing.

### Acceptance Criteria

- [x] The rendered page screenshot is taken
- [x] The browser URL changes as the rendered page changes

### Constraints

- None

### Resources

- None

## Handoff
- **What was done:** Updated preview URL state synchronization in `Preview.tsx` so Tauri web route changes (postMessage + iframe load) update both the URL bar state and the screenshot capture URL. This ensures captures use the currently rendered route instead of stale `/`.
- **Commit:** b7fb553
- **Files touched:** `app/components/preview/Preview.tsx`
- **Decisions made:** Kept the fix scoped to shared URL-state syncing (`syncPreviewUrlState`) used by navigation events, route messages, and manual URL entry.
- **Known limitations:** Full repo lint still fails due many unrelated pre-existing issues; file-level eslint on `Preview.tsx` passes with warnings only.
- **How to verify:** 1) Open a web preview at `/`. 2) Navigate in-app to `/pricing` (without typing in URL bar). 3) Confirm preview URL updates to `/pricing`. 4) Click camera and confirm screenshot reflects `/pricing`.

