Store most metadata in projects.json via the sidecar, as requested.

## Progress
- [x] Move deployment history from localStorage → projects.json (sidecar CRUD routes)
- [x] Add one-time migration from localStorage on app startup
- [x] Add launch config cache route (PUT /:id/launch-config)
- [x] Persist agent session token usage and cost to projects.json
- [x] Add deployment CRUD bridge methods in conveyor-bridge
- [x] Update types (Project, AgentSession) to include new fields

## Decisions
- Deployments are persisted optimistically (in-memory update + fire-and-forget sidecar write)
- Launch config in projects.json is a cache; launch.json file remains source of truth
- Token/cost updates are fire-and-forget to avoid blocking the agent stream

## Outcome
Deployments, launch config, and token usage are now stored in projects.json via the sidecar. localStorage is migrated once on startup.

Commits: 228c466, 866612d