1. Do we store these credentials and use them later for onboarding
	1. Just like the way the user can set up an integration
	2. SSO is the only thing expo supports
	3. User is just redirected to login
2. Onboarding we ask the user to set up locally
	1. We log in to expo locally too, we can then reuse these credentials during deployment
3. I can retain the expo step, we can check if a user is already logged in and simply reuse that login
4. After the user starts a build, 

---

Next step

1. `npx testflight` after set up
2. Managing agent sessions
	1. We had some consistency before
	2. Claude code sessions
3. `UIMessage` as the uniform storage format
4. We need to have a layer that transforms both Codex and Claude Code encoding formats to `UIMessage`
	1. We use message parts to store the messages
	2. 