I think we should move more of the logic in the system prompt to `AGENTS.md` and `CLAUDE.md` as agents actually respect these files and they don't require injecting logic into the system prompt.

Say we don't want users to snipe our custom instructions, how do we handle this?

## Progress
- [x] Added sidecar instruction policy engine for hard enforcement (non-overridable by default)
- [x] Integrated policy checks into agent tool-call stream path
- [x] Added managed-file tamper protection for AGENTS.md / CLAUDE.md
- [x] Added deprecated package install blocking
- [x] Added per-project override toggle in Project Settings (`allowInstructionOverride`)
- [x] Migrated most behavioral guidance from runtime system prompt into managed AGENTS/CLAUDE file generation
- [x] Added tests for policy decisions and verified existing scaffold block test still passes

## Decisions
- Enforce anti-sniping on the sidecar (tool-call gate), not only in prompts.
- Keep default strict mode: bypass disabled unless project-level override is explicitly enabled.
- Treat AGENTS.md and CLAUDE.md as fully managed and block agent edits in strict mode.
- Keep runtime prompt minimal and use it for bootstrap/runtime hints; move broad behavior guidance to managed files.

## Outcome
Implemented and verified.

Targeted tests passing:
- `bun test src/services/agent-session.test.ts src/services/instruction-policy.test.ts`

Commit: pending

## Progress Update (scope correction)
- Reverted over-scoped changes (policy engine, prompt refactor, UI/schema changes).
- Implemented only generated-app requirement: `/api/template/initialize` now syncs `AGENTS.md` and `CLAUDE.md` after template copy.
- If instruction-file sync fails, endpoint now returns failure so generated apps are guaranteed to include both files on success.

## Outcome Update
- Remaining local changes from this task: `packages/sidecar/src/routes/template.ts` only.
- Verification:
  - Sidecar tests pass (`agent-session`, `workspace-profile`).
  - Lint on `template.ts` has warnings only (pre-existing `console.log` style warnings), no errors.
